# UI 制作ツールの要件メモ

フロントエンドの見た目を作るための GUI ツールに欲しい要件をまとめたもの。
**このリポジトリでは実装しない**。将来ツールを作る／探すときの判断材料。

要件はどれも、このプロジェクトの実作業で実際に詰まった箇所から起こしている。
「あると便利そう」ではなく「無くて事故った」を優先して並べた。

---

## 1. なぜ必要か

現状のループはこうなっている。

```
CSS を編集 → HMR を待つ → スクリーンショットを撮る → 切り出して拡大 → 目で見る
```

1往復が数十秒かかるうえ、**見たいものが見えるとは限らない**。実際に起きたこと:

- 記事カードの影が隙間を埋めてウィンドウ全体に黒いベールを作っていたのに、
  ウィンドウを切り抜いたスクショでは「そういう配色」に見えて気づけなかった。
  結局、ウィンドウ表示時と非表示時の全画面キャプチャを撮って輝度比を出す
  スクリプトを書いて特定した（CLAUDE.md の「透過の確認は目視ではなくピクセル差分で」）
- アニメーションに `fill-mode` を付け忘れ、終了後にパネル中央へ橙色の罫線が
  residue として残っていた。320ms のアニメーションの**終了後の状態**を見る手段が
  無かったため、パネル中央のピクセルを実測して初めて発覚した
- 圧縮された参考資料から細い枠線の有無を実測で読もうとして、2回誤った実装をした。
  必要だったのは測定ではなく**資料と実物を並べて人間が形を見比べること**だった

つまり欲しいのは「もっと速い編集」だけではなく、
**見たい状態を確実に画面に出せること**と**資料と突き合わせられること**。

---

## 2. スコープ

**やること**

- 見た目（レイアウト・配色・形状・モーション）の制作と調整
- 結果を**このリポジトリのソースへ書き戻す**

**やらないこと**

- ロジック・状態管理・データ層の編集
- 実行時にアプリへ同梱されるもの（開発時専用であること）

**書き戻しが必須要件である理由**: DevTools で調整しても、値を手で写す作業が残り、
そこで写し間違いと「なぜその値なのか」の消失が起きる。ツールが吐いた差分を
そのままレビューできないなら、往復は減らない。

---

## 3. 前提（このプロジェクト固有の制約）

ツールがこれらを理解していないと、正しく見えているのに実物では壊れる。

| 制約 | 内容 |
| --- | --- |
| スキントークン | 配色は `App.tsx` ルートの CSS カスタムプロパティ（`--panel-rgb-*`, `--accent-rgb-*` 等）から継承で伝わる。`createPortal` で `document.body` 直下に出す要素（`FeedPicker` / `FontPicker` のポップアップ）はこの継承外 |
| 不透明度 | `.panel-bg` は常に不透明。ウィンドウ透過は Win32 のレイヤードウィンドウ側。ただし浮遊モード（`Skin.floating`）だけは例外で CSS の alpha が本当にデスクトップまで効く |
| 仮想リスト | 記事行は仮想化されている。カード高は固定値で、`EntryList` の `estimateSize` と一致していないと行が重なる |
| `backdrop-filter` | 不透明な `.panel-bg` の上では視覚効果ゼロ。浮遊モードでのみ意味を持つ |
| Tailwind v4 | ユーティリティは `@layer utilities`。`index.css` の非レイヤー規則が常に勝つ |

---

## 4. 機能要件

優先度は **A（無いと同じ事故が再発する）/ B（往復が大きく減る）/ C（あると良い）**。

### 4.1 参考資料との突き合わせ — A

- 参考画像を読み込み、**実物の上に重ねて表示**（不透明度可変）、**横並び**、**差分**の3モード
- 定規、グリッド、スポイト
- **スポイトは資料の圧縮形式を警告すること。** JPEG も h.264 も既定で 4:2:0 の
  クロマサブサンプリングをかけ、色差だけ解像度が半分になる。高コントラストの境界では
  色が滲んで**存在しない色付きの縁が見える**。このプロジェクトではこれを枠線と誤読して
  2回実装している
- 設計方針: **形状の判断は人間がやる。ツールは比較を提供するだけで、結論を出さない。**
  配色は多少ずれても成立するが、形状・寸法・縁の有無は資料と測定の精度がそのまま出る

### 4.2 アニメーション編集 — A

- **タイムラインとスクラブ。** 任意のフレームで停止して見られること
- **アニメーション終了後の静止状態を明示すること。** `fill-mode` の有無で結果が変わる。
  「このアニメーションを取り除いたらこの要素はどう見えるか」を1操作で確認できること
- キーフレームのオフセットが単調増加であることの検証（順序が逆転すると
  `element.animate()` は TypeError を投げ、React ツリーごと落ちる。実際に起きた）
- **スタガーの生成。** 「N行を M ms 間隔でずらす」を指定したら CSS を吐く。
  今は `:nth-child` の規則を手書きで8個並べている
- `prefers-reduced-motion` のプレビュー切り替え
- **イージングとフレームレートの干渉の警告。** `steps()` の刻みが 60fps と近接して
  ずれていると、干渉して不規則にガタつく（`MarqueeTitle` で実際に踏んだ）
- 合成レイヤーの可視化。`transform` と `translate`/`scale` の個別プロパティのどちらを
  使っているか、`backdrop-filter` 配下でメインスレッド再描画に落ちていないか

### 4.3 状態のプレビュー — A

手で再現するのが面倒な状態を1クリックで出せること。

- 空・読み込み中・エラー・初回起動（フィード0件）
- 検索ヒット0件、ブックマーク0件
- 仮想リストの各表示モード × カードサイズ × 間隔
- 待機（アイドル）状態。**今は3分待つしかない**
- 長いタイトル、サムネイル欠落、favicon 未取得、極端に長い本文

### 4.4 透明ウィンドウのプレビュー — A

- 任意の背景（明るい壁紙・暗い壁紙・情報量の多い壁紙）の上でのプレビュー
- **合成後の実測。** ウィンドウ有無の差分を取って「本当に透けているか」を数値で出す。
  現状は都度スクリプトを書いている
- ウィンドウ不透明度スライダーの値を反映したプレビュー

### 4.5 配色とコントラスト — B

- 全スキン × ライト/ダークのトークンを一覧編集
- **このプロジェクトの規約を理解した検算。** アクセント色は2つの役割を同時に負う:
  1. `.panel-bg` の上に描くアイコン/ラベル（テキスト扱いで 4.5:1 目標）
  2. `.accent-bg` のベタ塗りに載る文字（ライトは白、ダークはパネル色に反転）
  片方だけ見て通したことが実際にあるので、**両方を常時表示**すること
- 「明るいアクセントは白文字を支えられない」を編集中に気づける表示
- 補色は高コントラストを意味しない（輝度で決まる）ことが分かる表示

### 4.6 UI ブロックの直接編集 — B

- 余白・寸法・角丸・枠・影を直接操作し、**Tailwind のクラス文字列として書き戻す**
- 素の CSS が要る箇所（`index.css`）とユーティリティで足りる箇所の区別
- **ショートハンドの二重宣言の検出。** `index.css` の `transition` と要素側の
  `transition-all` を両方書くと、順序次第で片方が黙って上書きされる（実際に起きた）

### 4.7 SVG アイコン作成 — B

- `viewBox 0 0 16 16` のグリッド上での作図。既存の流儀（`strokeWidth 1.25`,
  `stroke="currentColor"`, `strokeLinecap="round"`）を既定値として持つ
- **等倍プレビューを既存アイコンの隣に並べること。** 16px という小さいキャンバスでは
  対称な2点＋曲線がすぐ「顔」に見える。実際にパレットアイコンで踏んで作り直した
- ライト/ダーク両方、および塗り潰し円の上での同時プレビュー
- `icons.tsx` の `<Icon>` ラッパー形式でエクスポート

### 4.8 差分レビュー — B

- 変更前後のスクリーンショットを並べる
- 変更が**意図した箇所だけに効いているか**の確認（スキン固有の変更が他スキンへ
  漏れていないか）。テーマ間の意図しない波及は実際に何度も起きている

### 4.9 その他 — C

- レスポンシブ（ウィンドウ幅 320px 〜）の同時プレビュー
- フォント差し替えのプレビュー（英数字と日本語で別フォントを指定する構成に対応）

---

## 5. 非機能要件

- **アプリ本体に実行時依存を持ち込まないこと。** 開発時専用
- 書き戻しは**レビュー可能な差分**として出ること。ソースを機械生成物にしない
- 既存のコメント（このリポジトリは「なぜそうしたか」をコメントに厚く残す方針）を
  破壊しないこと
- `npm run tauri dev` と共存できること。ポータブル版と dev を同時に起動すると
  `.window-state.json` を共有して位置を奪い合う問題があるので、同種の衝突を作らない

---

## 6. 未決事項

- **形態**: 独立したアプリか、アプリ内の開発用オーバーレイか。
  後者なら実物の DOM をそのまま触れるが、本体にコードが混入するリスクがある
- **書き戻しの安全性**: Tailwind のクラス文字列は JSX の中にあり、条件分岐で
  組み立てられている箇所も多い。機械的な書き換えをどこまで許すか
- **既存ツールで足りるか**: 4.5（コントラスト）と 4.7（アイコン）は既存ツールがある。
  自作が要るのは 4.1〜4.4 の、**このアプリの実際の描画結果**に踏み込む部分
- 誰が使うか。作者一人なら書き戻しの厳密さより往復の速さを優先してよい
