# UI 制作ツールの要件メモ

画面の見た目を作るための GUI ツールに欲しい要件。
**このリポジトリでは実装しない**。将来ツールを作る／探すときの判断材料。

このリポジトリに置いてあるが、**要件は特定のプロジェクトに縛らない**。
RSS-Widget での実例は根拠として §7 に付録としてまとめ、本文は汎用の要件にしてある。

---

## 1. 誰のための、何のツールか

**使う人はプログラミングをしない。** 見た目の判断はできるが、コードは書かないし読まない。
実装は AI か開発者が担当する。

この前提が要件の中心にある。したがって:

- **コードを読まずに見た目を作れること**が機能要件であって、親切心ではない
- ツールの出力は**そのまま実装に使えるもの**であること。使う人が値を口頭で伝え直したり、
  スクリーンショットで説明したりする段階が残るなら、往復は減らない
- 逆に、使う人が**コードをレビューする前提の設計にしないこと**。
  「差分を確認してください」は、この使い手には確認になっていない

**想定する流れ**

```
使う人がツールで見た目を作る
   ↓  ツールが「何をどう変えたか」を出力
AI または開発者が実装に反映する
   ↓
使う人がツール上で結果を見て、次の調整をする
```

つまりツールは**エディタであると同時に、実装への指示書を生成する装置**である。

---

## 2. スコープ

**やること**: 見た目（配置・寸法・配色・形・動き）の制作と調整、およびその出力

**やらないこと**: ロジック・データ・状態管理の編集。実行時にアプリへ同梱されるもの

---

## 3. 使う人が非プログラマであることから来る要件

ここが他のデザインツールとの差になる部分。優先度は最高。

### 3.1 コードを見せない

- 数値・色・形はすべて**視覚的な操作**で決められること。
  ハンドルを掴む、スライダーを動かす、色を選ぶ
- コードは「見たい人が開けば見える」場所に置く。既定では表示しない
- **専門用語をそのまま出さない。** 「余白」「角の丸み」「影の広がり」であって、
  クラス名やプロパティ名ではない

### 3.2 実装への引き渡しが第一級の機能

- 変更を**実装可能な精度**で出力する。「もう少し余白を広く」ではなく
  「上下の余白を 8px から 12px へ」
- 出力は**人間にも機械にも読める**こと。AI に渡してそのまま反映できる形式が要る
- **どの画面のどの要素か**が曖昧にならないこと。名前・スクリーンショット・
  位置のいずれかで一意に指せること
- 変更の**意図**を添えられること。「なぜそうしたか」は後から効いてくる。
  値だけ渡すと、実装側が別の解釈で似た値を入れることがある

### 3.3 壊れないこと

- **試すことと確定することを分ける。** いじっている間は何も壊れない
- いつでも直前・任意の時点へ戻せる。履歴が残る
- 「元のデザインに戻す」が1操作でできる

### 3.4 なぜそうなっているかを説明する

- ある値を選んだとき、**それがどこから来ているか**を示せること。
  「この色はテーマの設定から来ている」「この余白は全画面共通の設定」
- **共通設定を触ろうとしていることを警告すること。**
  1箇所だけ直したつもりが全画面に波及するのは、非プログラマが最も踏みやすい罠

---

## 4. 汎用の機能要件

優先度 **A / B / C**。A は「無いと同じ失敗を繰り返す」もの。

### 4.1 直接操作と即時反映 — A

- 実物の画面上で要素を選び、その場で寸法・余白・色・角丸・枠・影を変える
- 変更は**待たずに**反映される
- 選択中の要素が、画面上と構造の一覧の両方で分かること

### 4.2 状態を呼び出せる — A

作っている画面を、**手では再現しづらい状態**に1操作で切り替えられること。

- 空・読み込み中・エラー・初回起動
- 検索結果ゼロ、権限なし、オフライン
- 一定時間放置した状態（**待つしかない状態を待たずに出せること**）
- データが極端な場合（文字が長すぎる、画像が無い、件数が多すぎる／少なすぎる）
- ホバー・押下・選択中・無効といった、**マウスを離すと消えてしまう状態の固定**

> 実例: RSS-Widget の待機演出は 3 分放置しないと確認できず、確認のたびに
> 3 分待つか、コードの数値を一時的に書き換えるしかなかった。

### 4.3 参考資料との突き合わせ — A

- 参考画像を読み込み、**実物の上に重ねる**（不透明度可変）／**横に並べる**／
  **差分を見る**の3モード
- 定規、グリッド、色の吸い取り
- **資料の画質を警告すること。** 写真形式（JPEG）や動画は、色の情報だけ解像度を
  半分に落として保存する仕組みを使っている。そのため**明暗の境目に、実際には無い色が
  滲んで見える**。細い枠線や縁の有無を資料から読み取ろうとすると、ここで誤る
- **設計方針: 形の判断は人間がやる。ツールは見比べる手段を出すだけで、結論を出さない。**
  配色は多少ずれても成立するが、形・寸法・縁の有無は資料と測定の精度がそのまま結果に出る

> 実例: 圧縮された参考資料から「パネルに橙色の枠線がある」と読み取って実装し、
> 実際には存在しなかったため 2 回巻き戻した。必要だったのは測定ではなく、
> 資料と実物を並べて人間が形を見比べることだった。

### 4.4 動き（アニメーション）の編集 — A

- **タイムラインと再生位置の手動送り。** 任意の瞬間で止めて見られること
- **動きが終わった後の静止状態を明示すること。** 動きの設定次第で、終了後に
  元へ戻るか、最後の状態で止まるかが変わる。ここを間違えると**意図しない残留**が出る
- 「この動きを取り除いたら、この要素はどう見えるか」を1操作で確認できること
- **順番にずらして出す（スタガー）の生成。** 「N 個を M ミリ秒間隔でずらす」を
  指定するだけで済むこと
- 動きを減らす設定（OS の「アニメーションを減らす」）でのプレビュー
- **なめらかさの問題の検出。** 動きの刻みが画面の更新頻度と近接してずれていると、
  干渉して不規則にガタつく。目で見て原因を特定するのは難しい

> 実例: 動きの終了後の状態を確認する手段が無く、パネル中央に橙色の線が residue として
> 残ったまま気づかなかった。ピクセルを直接測って初めて発覚した。

### 4.5 スクリーンショットでは分からないものを可視化する — A

見た目の問題には、**画面を撮っても写らない**種類のものがある。

- 半透明の層が何枚重なっているか、どこにどれだけ掛かっているか
- 透明であるべき箇所が本当に透明か（背景を変えて比較する）
- 要素同士が重なっている／はみ出している
- 画面外や、隠れているのに存在し続けている要素

> 実例: カードの影が隙間を埋めてウィンドウ全体に薄い黒の膜を作っていたが、
> ウィンドウを切り抜いたスクリーンショットでは「そういう配色」に見えて気づけなかった。

### 4.6 配色とコントラスト — B

- 配色をまとめて編集できること（1色変えたら使っている全箇所に反映）
- **読みやすさの数値を常時表示すること。** 文字と背景の明暗差が足りているか
- **1つの色が複数の役割を持つ場合、その全部を同時に見せること。**
  「背景の上に置く文字色」として使う色が、同時に「塗りつぶしボタンの地色」でもある、
  というのはよくある。片方だけ見て通すと、もう片方で読めなくなる
- 補色は高コントラストを意味しない（明暗差で決まる）ことが分かる表示

### 4.7 図形・アイコンの作成 — B

- 小さいサイズ（16〜24px）のグリッド上での作図
- **等倍プレビューを既存のアイコンの隣に並べること。** 小さいキャンバスでは
  意図しない形に見えることがあり、拡大表示では気づけない
- 明るい背景・暗い背景・塗りつぶしの上での同時プレビュー
- プロジェクトの既定（線の太さ、角の処理）を引き継げること

> 実例: 16px のアイコンで、対称な2点と曲線の組み合わせが「顔」に見えてしまい作り直した。
> 拡大表示では最後まで気づかなかった。

### 4.8 変更の見比べ — B

- 変更前後を**視覚的に**並べる。コードの差分ではなく画面の差分
- 変更が**意図した箇所だけに効いているか**の確認。他の画面やテーマへの波及を検出

### 4.9 環境の違いのプレビュー — C

- ウィンドウ幅の違い、明暗テーマ、フォント差し替え
- 背景（壁紙）の違い。透過を使う画面では必須

---

## 5. 非機能要件

- **完成物に実行時の依存を持ち込まないこと。** 開発時専用
- 出力は**追跡でき、元に戻せる**形であること
- 既存のコードにある説明（なぜそうしたかの記述）を破壊しないこと
- 開発中のアプリと同時に動かせること

---

## 6. プロジェクト側が満たしておくべき前提

ツールが賢く働けるかは、対象プロジェクトの作りに依存する。新規プロジェクトでは
最初からこう作っておくと、後からツールを入れやすい。

- **配色・寸法などの共通設定が1箇所にまとまっている**こと（散らばっていると
  「1色変えたら全部に反映」ができない）
- 画面の状態（空・エラー・読み込み中）が**外から切り替えられる**こと
- 共通部品と、その画面だけのものが**区別できる**こと

---

## 7. 付録: RSS-Widget での実例と固有の制約

この文書の要件はここから起こしている。他プロジェクトへ持っていくときは
**この節は読み飛ばしてよい**。

### 7.1 詰まった事例

| 事例 | 対応する要件 |
| --- | --- |
| カードの影が重なりウィンドウ大の黒い膜になり、スクショで気づけなかった | 4.5 |
| 動きの終了後に橙色の線が残留。ピクセル実測で初めて発覚 | 4.4 |
| 圧縮資料から存在しない枠線を読み取り 2 回巻き戻した | 4.3 |
| 待機演出の確認に毎回 3 分待つ必要があった | 4.2 |
| 16px のアイコンが「顔」に見えて作り直した | 4.7 |
| アクセント色の 2 つの役割のうち片方だけ検算して通した | 4.6 |
| スキン固有の変更が他スキンへ漏れた | 4.8 |

### 7.2 このプロジェクト固有の制約

ツールがこれらを知らないと、ツール上では正しく見えて実物で壊れる。

| 制約 | 内容 |
| --- | --- |
| スキントークン | 配色はアプリのルート要素の CSS カスタムプロパティから継承で伝わる。`createPortal` で `document.body` 直下へ出す要素（`FeedPicker` / `FontPicker` のポップアップ）はこの継承外 |
| 不透明度 | `.panel-bg` は常に不透明で、ウィンドウ透過は Win32 のレイヤードウィンドウ側。ただし浮遊モード（`Skin.floating`）だけは例外で、CSS の alpha が本当にデスクトップまで効く |
| 仮想リスト | 記事行は仮想化されている。カード高は固定値で、`EntryList` の `estimateSize` と一致していないと行が重なる |
| `backdrop-filter` | 不透明な `.panel-bg` の上では視覚効果ゼロ。浮遊モードでのみ意味を持つ |
| Tailwind v4 | ユーティリティは `@layer utilities` に入る。`index.css` の非レイヤー規則が常に勝つ |
| ショートハンドの二重宣言 | `index.css` の `transition` と要素側の `transition-all` を両方書くと、順序次第で片方が黙って上書きされる |

---

## 8. 未決事項

- **形態**: 独立したアプリか、開発中のアプリに重ねる開発用オーバーレイか。
  後者は実物をそのまま触れる利点があるが、完成物にコードが混入するリスクがある
- **出力の形式**: 実装への指示をどう表現するか。自然言語の指示書か、機械可読の
  変更記述か、その両方か。§3.2 の中心的な問題
- **既存ツールで足りる範囲**: 4.6（配色）と 4.7（アイコン）は既存ツールがある。
  自作が要りそうなのは 4.2〜4.5 の、**実際に動いているアプリの描画結果**に
  踏み込む部分
- **対象プロジェクトの広さ**: Web だけでよいか、この種のデスクトップアプリまで含むか
