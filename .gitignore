# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-portable
gyroscope-portable
gyroscope-portable-v*.zip
*.local

# Legacy Claude settings may contain local permissions and credential-bearing command history.
context_archive/.claude/settings.local.json

# Local dev-server launch config for Claude Code's Browser pane preview
# (see "Tauriウィンドウ実機E2E" in context/VERIFY.md). Machine-local
# tooling, not project code -- kept intentionally untracked.
.claude/

# QA確認用に撮ったスクショなどを放り込む場所（フォルダごと無視）
qa-screenshots

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI共同開発用の内部文書（公開リポジトリには含めない）
context/
context_archive/
AGENTS.md
CLAUDE.md
NEXT_IMPLEMENTATION_INSTRUCTIONS.md

# ローカルのSkill接続と再生成できるGraphify出力
/.agents/
/graphify-out/
